import React from "react";
import { render } from "@testing-library/react-native";
import FacilityComponent from "./FacilityComponent";

describe('Facility Component', () => {
  test('should display text for the relevant facility', () => {
  });

  test('should display an icon for the relevant facility', () => {
  });
});
